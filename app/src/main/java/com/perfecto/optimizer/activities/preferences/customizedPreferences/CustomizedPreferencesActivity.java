package com.perfecto.optimizer.activities.preferences.customizedPreferences;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import com.perfecto.optimizer.R;

public class CustomizedPreferencesActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_customized_preferences);
    }
}
