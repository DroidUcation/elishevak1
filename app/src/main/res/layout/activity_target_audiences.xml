<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".preferences.targetAudience.TargetAudiencesActivity"
    android:background="@color/app_background">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="170dp"
        android:id="@+id/mapImageView"
        android:src="@drawable/map"
        android:background="@android:color/transparent"
        android:cropToPadding="false"
        android:contextClickable="false"
        android:contentDescription="map"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/target_audience_question"
        android:id="@+id/textView"
        android:layout_below="@+id/mapImageView"
        android:layout_centerHorizontal="true"
        android:textColor="@color/question_color"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/target_audience_button"
        android:id="@+id/button"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="87dp"
        android:width="50dp"
        android:textColor="@color/action_button_text_color"
        android:background="@color/action_button_background"
        android:onClick="selectCountry"/>
</RelativeLayout>
